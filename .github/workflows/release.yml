name: Release

on:
  release:
    types: [published]
  pull_request:
    branches: ["master"]

  workflow_dispatch:
    inputs:
      tag_name:
        description: "Release tag to upload assets to (e.g. test-v0.0.0). Create a draft/pre-release with this tag first."
        required: true
        default: "test-v0.0.0"

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  test:
    name: Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup MSBuild (find VS)
        if: runner.os == 'Windows'
        uses: microsoft/setup-msbuild@v2

      - name: Setup MSVC dev cmd
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Run tests
        run: cargo test --verbose

  build:
    name: Build & Upload (${{ matrix.target }})
    needs: [test]
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            archive: tar.gz
            bin_ext: ""

          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            archive: tar.gz
            bin_ext: ""

          - os: macos-15-intel
            target: x86_64-apple-darwin
            archive: tar.gz
            bin_ext: ""

          - os: macos-latest
            target: aarch64-apple-darwin
            archive: tar.gz
            bin_ext: ""

          # ✅ Windows (MSVC)
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            archive: zip
            bin_ext: ".exe"

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      # ✅ Ensure cl.exe + MSVC env vars exist on Windows
      - name: Setup MSVC dev cmd
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install musl tools (Linux musl only)
        if: matrix.os == 'ubuntu-latest' && matrix.target == 'x86_64-unknown-linux-musl'
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y musl-tools

      # ✅ Build (unix)
      - name: Build gibberish (release) [unix]
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          cargo build -p gibberish --release --target "${{ matrix.target }}"

      # ✅ Build (windows) — use pwsh, not bash
      - name: Build gibberish (release) [windows]
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          cargo build -p gibberish --release --target "${{ matrix.target }}"

      - name: Package (unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          BIN="gibberish"
          VERSION="${{ github.event.release.tag_name || inputs.tag_name }}"
          TARGET="${{ matrix.target }}"
          NAME="${BIN}-${VERSION}-${TARGET}"

          mkdir -p "dist/${NAME}"
          cp "target/${TARGET}/release/${BIN}" "dist/${NAME}/"

          tar -C dist -czf "dist/${NAME}.tar.gz" "${NAME}"

      - name: Package (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $bin = "gibberish${{ matrix.bin_ext }}"
          $version = "${{ github.event.release.tag_name || inputs.tag_name }}"
          $target = "${{ matrix.target }}"
          $name = "gibberish-$version-$target"

          New-Item -ItemType Directory -Force "dist\$name" | Out-Null
          Copy-Item "target\$target\release\$bin" "dist\$name\"
          Compress-Archive -Path "dist\$name" -DestinationPath "dist\$name.zip" -Force

      - name: Upload assets to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.release.tag_name || inputs.tag_name }}
          files: dist/**
