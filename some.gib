token whitespace = "\\s+";
token ident = "[a-zA-Z][a-zA-Z0-9]*";
token str = "'[^']*'";
token int = "[0-9]+";
token eq = "=";

parser expr = str | int;
parser name = ident;
parser stmt = name + eq + expr + whitespace;
parser root = stmt.repeated();
